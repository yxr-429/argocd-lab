apiVersion: v1
kind: Service
metadata:
  name: xinrui-service
  labels:
    app: testdeutsch
    label: quick
spec:
  selector:
    app: myapp
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
